require("evergarden").setup({
    theme = {
        variant = 'winter',
        accent = 'blue',
    },
    editor = {
        transparent_background = true,
    }
})

vim.cmd("colorscheme evergarden")
